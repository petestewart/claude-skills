study PLAN.md

pick the first unchecked task (- [ ]) in the list. your task is to implement it and validate that it works.

IMPORTANT:
- after making changes to the files, run the tests
- if any tests fail:
  - determine whether the test logic is no longer valid or whether it indicates a problem with the code.
  - if it indicates a problem with the code, make the necessary fix in the code.
  - if it indicates the test logic is no loner valid, update the test to correctly test the code
- your work is not done until the tests pass. if there is an environmental issue that is preventing tests from running (such as missing .env variables) it is your job to fix that so that tests can run.
- commit your work after it is completed and tests pass. Address any issues that are caught by pre-commit hooks
- update PLAN.md when your work is done to indicate the task's status
- when you have issues and need to troubleshoot, study these files
  - PROBLEM.md
  - PRD.md
  - specs/README.md
- again MAKE SURE that you commit your work when you are done.
